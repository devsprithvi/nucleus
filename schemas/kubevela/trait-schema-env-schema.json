{
  "default": {
    "containerName": "",
    "env": {},
    "replace": false,
    "unset": []
  },
  "oneOf": [
    {
      "required": [
        "containerName",
        "replace",
        "env",
        "unset"
      ]
    },
    {
      "required": [
        "containers"
      ]
    }
  ],
  "properties": {
    "containerName": {
      "default": "",
      "description": "Specify the name of the target container, if not set, use the component name",
      "title": "containerName",
      "type": "string"
    },
    "containers": {
      "description": "Specify the environment variables for multiple containers",
      "items": {
        "properties": {
          "containerName": {
            "default": "",
            "description": "Specify the name of the target container, if not set, use the component name",
            "title": "containerName",
            "type": "string"
          },
          "env": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Specify the  environment variables to merge, if key already existing, override its value",
            "title": "env",
            "type": "object"
          },
          "replace": {
            "default": false,
            "description": "Specify if replacing the whole environment settings for the container",
            "title": "replace",
            "type": "boolean"
          },
          "unset": {
            "description": "Specify which existing environment variables to unset",
            "items": {
              "type": "string"
            },
            "title": "unset",
            "type": "array"
          }
        },
        "required": [
          "containerName",
          "replace",
          "env",
          "unset"
        ],
        "type": "object"
      },
      "title": "containers",
      "type": "array"
    },
    "env": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Specify the  environment variables to merge, if key already existing, override its value",
      "title": "env",
      "type": "object"
    },
    "replace": {
      "default": false,
      "description": "Specify if replacing the whole environment settings for the container",
      "title": "replace",
      "type": "boolean"
    },
    "unset": {
      "description": "Specify which existing environment variables to unset",
      "items": {
        "type": "string"
      },
      "title": "unset",
      "type": "array"
    }
  },
  "type": "object"
}