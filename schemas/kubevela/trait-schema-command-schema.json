{
  "default": {
    "addArgs": null,
    "args": null,
    "command": null,
    "containerName": "",
    "delArgs": null
  },
  "oneOf": [
    {
      "required": [
        "containerName",
        "command",
        "args",
        "addArgs",
        "delArgs"
      ]
    },
    {
      "required": [
        "containers"
      ]
    }
  ],
  "properties": {
    "addArgs": {
      "description": "Specify the args to add in the target container, existing args will be kept, cannot be used with `args`",
      "items": {
        "type": "string"
      },
      "nullable": true,
      "title": "addArgs",
      "type": "array"
    },
    "args": {
      "description": "Specify the args to use in the target container, if set, it will override existing args",
      "items": {
        "type": "string"
      },
      "nullable": true,
      "title": "args",
      "type": "array"
    },
    "command": {
      "description": "Specify the command to use in the target container, if not set, it will not be changed",
      "items": {
        "type": "string"
      },
      "nullable": true,
      "title": "command",
      "type": "array"
    },
    "containerName": {
      "default": "",
      "description": "Specify the name of the target container, if not set, use the component name",
      "title": "containerName",
      "type": "string"
    },
    "containers": {
      "description": "Specify the commands for multiple containers",
      "items": {
        "properties": {
          "addArgs": {
            "description": "Specify the args to add in the target container, existing args will be kept, cannot be used with `args`",
            "items": {
              "type": "string"
            },
            "nullable": true,
            "title": "addArgs",
            "type": "array"
          },
          "args": {
            "description": "Specify the args to use in the target container, if set, it will override existing args",
            "items": {
              "type": "string"
            },
            "nullable": true,
            "title": "args",
            "type": "array"
          },
          "command": {
            "description": "Specify the command to use in the target container, if not set, it will not be changed",
            "items": {
              "type": "string"
            },
            "nullable": true,
            "title": "command",
            "type": "array"
          },
          "containerName": {
            "default": "",
            "description": "Specify the name of the target container, if not set, use the component name",
            "title": "containerName",
            "type": "string"
          },
          "delArgs": {
            "description": "Specify the existing args to delete in the target container, cannot be used with `args`",
            "items": {
              "type": "string"
            },
            "nullable": true,
            "title": "delArgs",
            "type": "array"
          }
        },
        "required": [
          "containerName",
          "command",
          "args",
          "addArgs",
          "delArgs"
        ],
        "type": "object"
      },
      "title": "containers",
      "type": "array"
    },
    "delArgs": {
      "description": "Specify the existing args to delete in the target container, cannot be used with `args`",
      "items": {
        "type": "string"
      },
      "nullable": true,
      "title": "delArgs",
      "type": "array"
    }
  },
  "type": "object"
}