{
  "properties": {
    "namespace": {
      "description": "Specify the namespace of the config.",
      "title": "namespace",
      "type": "string"
    },
    "template": {
      "description": "Specify the template of the config.",
      "title": "template",
      "type": "string"
    }
  },
  "required": [
    "template",
    "namespace"
  ],
  "type": "object"
}