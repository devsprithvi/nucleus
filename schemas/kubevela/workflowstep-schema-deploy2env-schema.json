{
  "properties": {
    "env": {
      "description": "Declare the name of the env in policy",
      "title": "env",
      "type": "string"
    },
    "parallel": {
      "default": false,
      "description": "components are applied in parallel",
      "title": "parallel",
      "type": "boolean"
    },
    "policy": {
      "default": "",
      "description": "Declare the name of the env-binding policy, if empty, the first env-binding policy will be used",
      "title": "policy",
      "type": "string"
    }
  },
  "required": [
    "policy",
    "env",
    "parallel"
  ],
  "type": "object"
}