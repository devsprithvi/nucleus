{
  "properties": {
    "appMountPath": {
      "description": "Specify the mount path of app container",
      "title": "appMountPath",
      "type": "string"
    },
    "args": {
      "description": "Specify the args run in the init container",
      "items": {
        "type": "string"
      },
      "title": "args",
      "type": "array"
    },
    "cmd": {
      "description": "Specify the commands run in the init container",
      "items": {
        "type": "string"
      },
      "title": "cmd",
      "type": "array"
    },
    "env": {
      "description": "Specify the env run in the init container",
      "items": {
        "properties": {
          "name": {
            "description": "Environment variable name",
            "title": "name",
            "type": "string"
          },
          "value": {
            "description": "The value of the environment variable",
            "title": "value",
            "type": "string"
          },
          "valueFrom": {
            "description": "Specifies a source the value of this var should come from",
            "properties": {
              "configMapKeyRef": {
                "description": "Selects a key of a config map in the pod's namespace",
                "properties": {
                  "key": {
                    "description": "The key of the config map to select from. Must be a valid secret key",
                    "title": "key",
                    "type": "string"
                  },
                  "name": {
                    "description": "The name of the config map in the pod's namespace to select from",
                    "title": "name",
                    "type": "string"
                  }
                },
                "required": [
                  "name",
                  "key"
                ],
                "title": "configMapKeyRef",
                "type": "object"
              },
              "secretKeyRef": {
                "description": "Selects a key of a secret in the pod's namespace",
                "properties": {
                  "key": {
                    "description": "The key of the secret to select from. Must be a valid secret key",
                    "title": "key",
                    "type": "string"
                  },
                  "name": {
                    "description": "The name of the secret in the pod's namespace to select from",
                    "title": "name",
                    "type": "string"
                  }
                },
                "required": [
                  "name",
                  "key"
                ],
                "title": "secretKeyRef",
                "type": "object"
              }
            },
            "title": "valueFrom",
            "type": "object"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "title": "env",
      "type": "array"
    },
    "extraVolumeMounts": {
      "description": "Specify the extra volume mounts for the init container",
      "items": {
        "properties": {
          "mountPath": {
            "description": "The mountPath for mount in the init container",
            "title": "mountPath",
            "type": "string"
          },
          "name": {
            "description": "The name of the volume to be mounted",
            "title": "name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "mountPath"
        ],
        "type": "object"
      },
      "title": "extraVolumeMounts",
      "type": "array"
    },
    "image": {
      "description": "Specify the image of init container",
      "title": "image",
      "type": "string"
    },
    "imagePullPolicy": {
      "default": "IfNotPresent",
      "description": "Specify image pull policy for your service",
      "enum": [
        "IfNotPresent",
        "Always",
        "Never"
      ],
      "title": "imagePullPolicy",
      "type": "string"
    },
    "initMountPath": {
      "description": "Specify the mount path of init container",
      "title": "initMountPath",
      "type": "string"
    },
    "mountName": {
      "default": "workdir",
      "description": "Specify the mount name of shared volume",
      "title": "mountName",
      "type": "string"
    },
    "name": {
      "description": "Specify the name of init container",
      "title": "name",
      "type": "string"
    }
  },
  "required": [
    "name",
    "image",
    "imagePullPolicy",
    "mountName",
    "appMountPath",
    "initMountPath",
    "extraVolumeMounts"
  ],
  "type": "object"
}